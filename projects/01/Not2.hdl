/**
 * 2-bit Not:
 * for i=0..1: out[i] = not in[i]
 */

CHIP Not2 {
    IN in[2];
    OUT out[2];

    PARTS:
    // Put your code here:
    Not(in=in[0], out=out[0]);
    Not(in=in[1], out=out[1]);

}